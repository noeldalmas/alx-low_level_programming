#include "main.h"

/**
 * _strcat - concatenate two strings
 * @dest: string 1
 * @src: string 2
 * Return: Concatenated string
 */
char *_strcat(char *dest, char *src)
{
	int i;
	int j;

	i = 0;
	while (dest[i] != '\0')
}